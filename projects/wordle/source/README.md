# C++ Wordle analyzer

Original C++17 implementation. Run `cmake -S . -B build`, `cmake --build build`, and `ctest --test-dir build --output-on-failure`. Run the executable from this directory or supply a dictionary path. fives.txt here is a newly assembled six-word demonstration list, replacing the original dictionary of unknown provenance.
